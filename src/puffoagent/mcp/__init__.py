"""MCP servers exposing puffoagent capabilities to AI agents.

The ``puffo_tools`` module is the primary one — a FastMCP stdio
server that lets agents proactively post to Puffo.ai channels, upload
files, and (for cli-local) proxy tool-permission requests back to
their human owner. See ``puffo_tools.py`` for the tool list.
"""
